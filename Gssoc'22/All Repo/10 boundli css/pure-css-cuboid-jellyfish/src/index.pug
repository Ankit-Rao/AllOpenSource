- let n = 15, m = n*n;

style .a3d { --n: #{n}; --i: 0; --j: 0 }
	- for(let i = 1; i < n; i++)
		| .s3d:nth-child(#{n}n + #{i + 1}) { --j: #{i} }
		| .s3d:nth-child(n + #{n*i + 1}) { --i: #{i} }
.a3d
	- for(let i = 0; i < n; i++)
		- for(let j = 0; j < n; j++)
			.s3d